#include "MainExport.h"
#include <ThreadPool.h>
#include "MediaFile.h"
bool TestExample::testMainFunc(std::string vedioFile, std::string audioFile, std::string outputPath)
{
    MediaFile buffer("", AVMEDIA_TYPE_AUDIO);
    return vedioFile.empty();

}
